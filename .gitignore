# dependencies / build
node_modules/
dist/
*.tsbuildinfo

# secrets — never commit
.env
.env.*
!.env.example

# tooling artifacts
.DS_Store
.vercel
.playwright-mcp/
.zcode/

# local DB migration snapshots (db-migration-engineer backups)
.db-snapshots/
